<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <header>
    <nav>
      <a href="/home">Home</a>
      <a href="/dashboard" class="active">Dashboard</a>
      <a href="/logout">Logout</a>
    </nav>
  </header>

  <main>
    <h1>Your Posts</h1>
    <button id="create-new-post">Create New Post</button>

    <section class="posts">
      {{#if posts.length}}
        {{#each posts}}
          <div class="post">
            <h2>{{this.title}}</h2>
            <p>Posted on: {{this.createdAt}}</p>
            <button class="edit-post" data-id="{{this.id}}">Edit</button>
            <button class="delete-post" data-id="{{this.id}}">Delete</button>
          </div>
        {{/each}}
      {{else}}
        <p>No posts yet. Create a new one!</p>
      {{/if}}
    </section>
  </main>

  <footer>
    <p>&copy; 2024 Tech Blog</p>
  </footer>

  <script src="/js/dashboard.js"></script>
</body>
</html>
